# Docker Platform Configuration
# Set this based on your system architecture (linux/arm64 for M1/M2 Macs, linux/amd64 for Intel)
CODE_SERVER_PLATFORM=linux/arm64

# Code-Server Password (required for VSCode web interface)
CODE_PASSWORD=your_secure_password_here
SUDO_PASSWORD=your_secure_sudo_password_here

# SSH Configuration (for terminal access to host from code-server)
# Path to SSH keys directory (default: ~/.ssh)
# Keys will be mounted read-only into code-server container
SSH_KEY_PATH=~/.ssh

# Workspace Path (where code-server will access files)
WORKSPACE_PATH=/config/workspace

# GitHub OAuth (for vibe-kanban integration)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Application Ports
FRONTEND_PORT=3000
BACKEND_PORT=8080

# Public URL (accessed through Caddy reverse proxy)
PUBLIC_SITE_URL=http://localhost:3001

# Development Settings
NODE_ENV=development
RUST_LOG=debug
DISABLE_WORKTREE_ORPHAN_CLEANUP=1
